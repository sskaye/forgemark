// Injected by scripts/build-cli.mjs via esbuild `define`. Absent when
// the CLI runs from source (vite-node), where main.ts falls back to "dev".
declare const __FORGEMARK_VERSION__: string | undefined;
